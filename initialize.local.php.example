<?php
/**
 * Optional local overrides (copy to initialize.local.php).
 * Forces local environment even when testing on a LAN IP.
 */
// define('APP_ENV', 'local');
// define('DB_SERVER', 'localhost');
// define('DB_USERNAME', 'root');
// define('DB_PASSWORD', '');
// define('DB_NAME', 'cbpos_db');
// define('base_url', 'http://localhost/cbpos/');

/**
 * Render / Docker: set DB_* in Render dashboard or .env (do not commit .env).
 * InfinityFree: use initialize.production.php for DB password only.
 */
