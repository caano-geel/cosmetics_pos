# Copy to `.env` for production (Render) or local Docker testing.
# Do not commit `.env` — use `.env.render` for Render (see DEPLOY_RENDER_ENV.md).

APP_ENV=production

DB_HOST=sql305.infinityfree.com
DB_NAME=if0_42288113_cbpos_db
DB_USER=if0_42288113
DB_PASSWORD=your_database_password

# Optional: force canonical site URL
# APP_URL=https://your-app.onrender.com
