# Environment variables (Render / Docker)
# Copy to `.env` for local Docker testing only — never commit `.env`

APP_ENV=production

DB_HOST=mysql-2970c59f-cosmetics-pos-d042.h.aivencloud.com
DB_PORT=18825
DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASSWORD=your_aiven_password

DB_SSL=true
DB_SSL_CA=certs/aiven-ca.pem

# Optional: force canonical site URL
# APP_URL=https://your-app.onrender.com
